<nav class="hidden" data-controller="bridge--menu" aria-hidden="true">
  <% navigation_items.each do |item| %>
    <%= link_to item[:name],
        item[:path],
        data: {
          bridge__menu_target: "item",
          bridge_title: item[:name]
        } %>
  <% end %>
</nav>
