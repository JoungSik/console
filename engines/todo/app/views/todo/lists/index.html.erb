<div class="w-full">
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-white">할 일 목록</h1>
    <%= link_to todo.new_list_path, class: "inline-flex items-center px-4 py-2 text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 dark:hover:bg-blue-500 transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 dark:focus:ring-offset-gray-800" do %>
      새 할 일 목록
    <% end %>
  </div>

  <div id="lists">
    <% if @lists.any? %>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3 sm:gap-4 lg:gap-6">
        <% @lists.each do |list| %>
          <div class="group relative">
            <%= link_to todo.list_path(list), class: "block hover:scale-105 transition-transform duration-200" do %>
              <%= render "todo/lists/list", list: list %>
            <% end %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="text-center py-12">
        <svg class="mx-auto h-12 w-12 text-gray-400 dark:text-gray-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
        </svg>
        <h3 class="mt-2 text-sm font-medium text-gray-900 dark:text-white">할 일 목록이 없습니다</h3>
        <p class="mt-1 text-sm text-gray-500 dark:text-gray-400">첫 번째 할 일 목록을 만들어보세요.</p>
      </div>
    <% end %>
  </div>
</div>
